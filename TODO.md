# TODO

## Tareas pendientes

- [ ] En `ej1`, cambiar la métrica principal de evaluación a `accuracy` para selección, resúmenes y comparación de experimentos.
- [ ] En `ej1`, mantener `precision`, `recall` y `F1` como métricas complementarias de análisis, no como criterio principal.
- [ ] En `ej1`, sacar `AUC-PR` de los gráficos y resúmenes principales para alinearlo con `ej2`.
- [ ] En `ej1`, elegir el umbral final del modelo usando una métrica de costo que priorice evitar `FN`, con costo `2*FN + FP`.

## Decisiones tomadas

- En `ej1`, para medir el punto `1.2`, usar `F2` como métrica principal.
